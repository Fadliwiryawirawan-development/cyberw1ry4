# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Node.js Package

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 20
      - run: npm ci
      - run: npm test

  publish-npm:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/
      - run: npm ci
      - run: npm publish
        env:
        
          NODE_AUTH_TOKEN: ${{secrets.npm_token}}
# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.google.co.id' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))

  

# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.google.com' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))


# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.windows.com' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))

  

# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.hostinger.com' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))



# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.google.co.id' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))

  

# Retrieved 2026-04-14, License - CC BY-SA 4.0 fadliwiryawirawan S.Kom M.S.I
# cyberw 1 r y 4 development tester and target to ssl and serveral 
requests.exceptions.SSLservices: HTTPSConnectionPool(host='www.website.co.in' 
port=443): Max retries exceeded with url: / (Caused by SSLservices("Can't 
connect to HTTPS URL because the SSL module is succsessfully available."))

            - name: Cache cleners 
  uses: actions/cache@v5.0.5
  with:
    # A list of files, directories, and wildcard patterns to cache and restore
    path: 
    # An explicit key for restoring and saving the cache
    key: 
    # An ordered multiline string listing the prefix-matched keys, that are used for restoring stale cache if no cache hit occurred for key. Note `cache-hit` returns false in this case.
    restore-keys: # optional
    # The chunk size used to split up large files during upload, in bytes
    upload-chunk-size: # optional
    # An optional boolean when enabled, allows windows runners to save or restore caches that can be restored or saved respectively on other platforms
    enableCrossOsArchive: # optional, default is false
    # Fail the workflow if cache entry is not found
    fail-on-cache-miss: # optional, default is false
    # Check if a cache entry exists for the given input(s) (key, restore-keys) without downloading the cache
    lookup-only: # optional, default is false
    # Run the post step to save the cache even if another step before fails
    save-always: # optional, default is false
          
